//! subsequnce is a sequence that can be derived from an array by selecting zero or more elements without changing the order of remaning elements.
//! its not continuous but still its order should not be changed. 
//! no. of subsequences of an array with n elements = 2^n.
//* every subarray is a subsequence but every subsequence is not a subarray

#include<iostream>
using namespace std;
int main(){

    return 0;
}